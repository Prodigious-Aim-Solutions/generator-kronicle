language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-kronicle' ]; then cd .. && eval "mv $currentfolder generator-kronicle" && cd generator-kronicle; fi

